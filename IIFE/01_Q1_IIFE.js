// Whate is an IIFE in JavaScript
// => IIFE (Immediately Invoked Function Expression) is a function
// that is executed immediately after it is defined.

(function () {
  console.log("IFEE is executed");
})();

// How is an IIFE written in JavaScript?
// =>
(function () {
  console.log("IIFE IS Runned Successfully");
})(); // The parentheses () at the end invoke the
//  function immediately after its definition.
